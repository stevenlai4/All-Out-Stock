import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    root: {
        borderWidth: 2,
        borderRadius: 8,
        marginVertical: 10,
        padding: 20,
    },
    company: {
        marginBottom: 10,
        fontSize: 18,
        fontWeight: "bold"
    },
});
